name: 🏗️ Build Kernel (G ReSukiSU + Custom + SUSFS)

on:
  workflow_dispatch:

jobs:
  build-kernel:
    name: 🏗️ Build s5e8535 / M14
    runs-on: ubuntu-22.04
    timeout-minutes: 180

    env:
      SUSFS_BRANCH: "gki-android13-5.15"
      SUSFS_URL: "https://gitlab.com/simonpunk/susfs4ksu.git"
      CUSTOM_PATCH_URL: "https://raw.githubusercontent.com/MrPankaj24/susfs-SM-M146B/main/50_add_susfs_in_gki-android13-5.15.patch"
      CUSTOM_FRAG_URL: "https://raw.githubusercontent.com/MrPankaj24/susfs-SM-M146B/main/defconfig.fragment"

    steps:
      - name: 📥 Checkout Kernel Source
        uses: actions/checkout@v4
        with:
          repository: MrPankaj24/SM-M146B-Kernel-Source
          ref: main
          fetch-depth: 1

      - name: 📦 Install Build Dependencies
        run: |
          sudo apt-get update -y
          sudo apt-get install -y \
            git curl wget bc bison flex libssl-dev libelf-dev python3 python-is-python3 \
            make gcc gcc-aarch64-linux-gnu binutils binutils-aarch64-linux-gnu \
            g++ cpio zip unzip lz4 zstd xz-utils libncurses-dev libncurses5-dev \
            kmod rsync lld llvm clang dwarves pahole ccache device-tree-compiler \
            zlib1g-dev libc6-dev-i386 cmake ninja-build

      - name: 🔧 Cache Toolchains
        id: cache-toolchain
        uses: actions/cache@v4
        with:
          path: |
            toolchain/clang/host/linux-x86/clang-r450784d
            toolchain/build/kernel/build-tools
            toolchain/prebuilts/kernel-build-tools
          key: toolchain-clang-r450784d-v1

      - name: ⬇️ Download Clang r450784d (AOSP)
        if: steps.cache-toolchain.outputs.cache-hit != 'true'
        run: |
          mkdir -p toolchain/clang/host/linux-x86/clang-r450784d
          curl -L "https://android.googlesource.com/platform/prebuilts/clang/host/linux-x86/+archive/refs/heads/android13-release/clang-r450784d.tar.gz" -o /tmp/clang.tar.gz
          tar -xzf /tmp/clang.tar.gz -C toolchain/clang/host/linux-x86/clang-r450784d
          rm /tmp/clang.tar.gz

      - name: ⬇️ Download Kernel Build Tools
        if: steps.cache-toolchain.outputs.cache-hit != 'true'
        run: |
          git clone --depth=1 -b main https://android.googlesource.com/kernel/prebuilts/build-tools toolchain/build/kernel/build-tools
          mkdir -p toolchain/build/kernel/build-tools/path
          ln -sf $(pwd)/toolchain/build/kernel/build-tools/linux-x86 toolchain/build/kernel/build-tools/path/linux-x86
          ln -sf $(pwd)/toolchain/build/kernel/build-tools/sysroots/x86_64-unknown-linux-musl toolchain/build/kernel/build-tools/sysroot

      - name: ⬇️ Download Prebuilt Tools (for pahole)
        if: steps.cache-toolchain.outputs.cache-hit != 'true'
        run: |
          git clone --depth=1 https://android.googlesource.com/kernel/prebuilts/build-tools toolchain/prebuilts/kernel-build-tools

      - name: 🥷 Clone SUSFS Repo
        run: |
          set -e
          git clone --depth=1 --single-branch --branch "${SUSFS_BRANCH}" "${SUSFS_URL}" /tmp/susfs4ksu

      - name: ⬇️ Download Custom SUSFS Patch & Fragment
        run: |
          curl -LSs "${CUSTOM_PATCH_URL}" -o /tmp/custom_susfs.patch
          curl -LSs "${CUSTOM_FRAG_URL}" -o /tmp/defconfig.fragment

      - name: 🛡️ Inject ReSukiSU
        run: |
          curl -LSs "https://raw.githubusercontent.com/ReSukiSU/ReSukiSU/sync-susfs/kernel/setup.sh" | bash

      - name: 📁 Copy missing files from SUSFS
        run: |
          set -euo pipefail
          PATCH_FILE="/tmp/custom_susfs.patch"
          grep '^diff --git' "$PATCH_FILE" | awk '{print $3, $4}' | sed 's#^a/##; s#^b/##; s# b/##g' | tr ' ' '\n' | sed '/^$/d' | sort -u > /tmp/patch_targets.txt || true
          while read -r f; do
            [ -z "$f" ] && continue
            candidate="/tmp/susfs4ksu/${f}"
            if [ ! -e "$f" ]; then
              if [ -e "$candidate" ]; then
                mkdir -p "$(dirname "$f")"
                cp --parents "$candidate" . || cp "$candidate" "$f"
              elif [ -e "/tmp/susfs4ksu/kernel_patches/$f" ]; then
                mkdir -p "$(dirname "$f")"
                cp --parents "/tmp/susfs4ksu/kernel_patches/$f" . || cp "/tmp/susfs4ksu/kernel_patches/$f" "$f"
              fi
            fi
          done < /tmp/patch_targets.txt || true
          cp -r /tmp/susfs4ksu/kernel_patches/fs/* fs/
          cp -r /tmp/susfs4ksu/kernel_patches/include/linux/* include/linux/

      - name: 🥷 Apply SUSFS patch
        run: |
          patch -p1 -l < /tmp/custom_susfs.patch

      - name: 🥷 Inject pagemap.h SUSFS flags
        run: |
          echo ">>> Injecting SUSFS flags into pagemap.h..."
          if ! grep -q 'AS_FLAGS_OPEN_REDIRECT' include/linux/pagemap.h; then
            sed -i '/AS_THP_SUPPORT = 6,/a\    AS_FLAGS_OPEN_REDIRECT    = 29,\n    AS_FLAGS_SUS_KSTAT        = 30,\n    AS_FLAGS_SUS_MAP          = 31,' include/linux/pagemap.h
          fi

      - name: 🔧 Add missing SUSFS function stubs
        run: |
          set -e
          echo ">>> Adding missing SUSFS function stubs for sync-susfs compatibility..."

          if ! grep -q 'susfs_get_non_sus_vfsmnt_from_vfsmnt' fs/susfs.c; then
            cat >> fs/susfs.c << 'STUBEOF'

/* Stubs for sync-susfs ReSukiSU compatibility */
#ifdef CONFIG_KSU_SUSFS_SUS_MOUNT
#include "mount.h"

struct vfsmount *susfs_get_non_sus_vfsmnt_from_vfsmnt(struct vfsmount *mnt)
{
    if (!mnt)
        return mnt;
    return mnt;
}

struct mount *susfs_get_non_sus_mnt_from_mnt(struct mount *mnt)
{
    if (!mnt)
        return mnt;
    return mnt;
}
#endif /* CONFIG_KSU_SUSFS_SUS_MOUNT */
STUBEOF
            echo "  ✅ Added function stubs to fs/susfs.c"
          else
            echo "  Functions already exist in susfs.c"
          fi

          echo ">>> Adding declarations to susfs.h..."
          if ! grep -q 'susfs_get_non_sus_vfsmnt_from_vfsmnt' include/linux/susfs.h; then
            cat >> include/linux/susfs.h << 'DECLEOF'

/* sync-susfs compatibility declarations */
#ifdef CONFIG_KSU_SUSFS_SUS_MOUNT
struct vfsmount;
struct mount;
extern struct vfsmount *susfs_get_non_sus_vfsmnt_from_vfsmnt(struct vfsmount *mnt);
extern struct mount *susfs_get_non_sus_mnt_from_mnt(struct mount *mnt);
#endif
DECLEOF
            echo "  ✅ Added declarations to susfs.h"
          fi

          echo ">>> Verifying:"
          grep -n 'susfs_get_non_sus' fs/susfs.c | head -5
          grep -n 'susfs_get_non_sus' include/linux/susfs.h | head -5

      - name: 🔨 Compile Kernel
        run: |
          set -e
          set -o pipefail

          # Prebuilt pahole MUST come first in PATH to fix BTF error
          export PATH=$(pwd)/toolchain/prebuilts/kernel-build-tools/linux-x86/bin:$PATH
          export PATH=$(pwd)/toolchain/clang/host/linux-x86/clang-r450784d/bin:$PATH
          export PATH=$(pwd)/toolchain/build/kernel/build-tools/path/linux-x86/:$PATH

          export HOSTCFLAGS="-I$(pwd)/toolchain/prebuilts/kernel-build-tools/linux-x86/include"
          export HOSTLDFLAGS="-Wl,-rpath,$(pwd)/toolchain/prebuilts/kernel-build-tools/linux-x86/lib64 -L$(pwd)/toolchain/prebuilts/kernel-build-tools/linux-x86/lib64 -fuse-ld=lld --rtlib=compiler-rt"
          export DTC_FLAGS="-@"
          export PLATFORM_VERSION=15
          export ANDROID_MAJOR_VERSION=v
          export LLVM=1
          export DEPMOD=depmod
          export ARCH=arm64
          export TARGET_SOC=s5e8535

          echo ">>> Checking pahole version..."
          which pahole
          pahole --version || true

          echo ">>> 1. Generating Base Config..."
          make s5e8535-m14xnsxx_defconfig

          echo ">>> 2. Injecting config..."
          cat >> .config << 'CFGEOF'
CONFIG_KSU=y
CONFIG_KPROBES=y
CONFIG_HAVE_KPROBES=y
CONFIG_KPROBE_EVENTS=y
CONFIG_THREAD_INFO_IN_TASK=y
CONFIG_KSU_SUSFS=y
CONFIG_KSU_SUSFS_SUS_PATH=y
CONFIG_KSU_SUSFS_SUS_MOUNT=y
CONFIG_KSU_SUSFS_SUS_KSTAT=y
CONFIG_KSU_SUSFS_SUS_KSTAT_REDIRECT=y
CONFIG_KSU_SUSFS_SUS_MAP=y
CONFIG_KSU_SUSFS_SPOOF_UNAME=y
CONFIG_KSU_SUSFS_ENABLE_LOG=y
CONFIG_KSU_SUSFS_SPOOF_CMDLINE_OR_BOOTCONFIG=y
CONFIG_KSU_SUSFS_OPEN_REDIRECT=y
CONFIG_KSU_SUSFS_HIDE_KSU_SUSFS_SYMBOLS=y
CONFIG_KSU_SUSFS_UNICODE_FILTER=y
CONFIG_KSU_SUSFS_AUTO_ADD_SUS_KSU_DEFAULT_MOUNT=y
CONFIG_KSU_SUSFS_AUTO_ADD_SUS_BIND_MOUNT=y
CONFIG_TMPFS_XATTR=y
CONFIG_TMPFS_POSIX_ACL=y
CFGEOF

          make olddefconfig

          echo ">>> Config check:"
          grep -E "CONFIG_KSU|CONFIG_DEBUG_INFO_BTF" .config | head -20

          echo ">>> 3. Compiling..."
          make -j$(nproc) 2>&1 | tee /tmp/kernel_build.log

      - name: 📦 Package with AnyKernel3
        if: success()
        run: |
          set -e
          git clone --depth=1 https://github.com/osm0sis/AnyKernel3.git /tmp/AnyKernel3
          rm -rf /tmp/AnyKernel3/.git /tmp/AnyKernel3/modules /tmp/AnyKernel3/patch
          rm -f /tmp/AnyKernel3/ramdisk/fstab.* 2>/dev/null || true

          cat > /tmp/AnyKernel3/anykernel.sh << 'AKEOF'
properties() { '
kernel.string=ReSukiSU+SUSFS for SM-M146B
do.devicecheck=1
do.modules=0
do.systemless=1
do.cleanup=1
do.cleanuponabort=0
device.name1=m14x
device.name2=m14xns
device.name3=SM-M146B
device.name4=s5e8535
supported.versions=13-15
supported.patchlevels=
'; }

block=/dev/block/by-name/boot;
is_slot_device=0;
ramdisk_compression=auto;
patch_vbmeta_flag=auto;

. tools/ak3-core.sh;
dump_boot;
write_boot;
AKEOF

          if [ -f arch/arm64/boot/Image ]; then
            cp arch/arm64/boot/Image /tmp/AnyKernel3/
            echo ">>> Copied Image"
          else
            echo "❌ No Image found!"
            ls -la arch/arm64/boot/
            exit 1
          fi

          if [ -d arch/arm64/boot/dts/exynos ]; then
            find arch/arm64/boot/dts/exynos -name "*.dtb" | while read dtb; do
              cp "$dtb" /tmp/AnyKernel3/ 2>/dev/null || true
            done
          fi

          cd /tmp/AnyKernel3
          zip -r9 /tmp/ReSukiSU-SUSFS-M14x-kernel.zip * -x '*.git*'
          mkdir -p ${GITHUB_WORKSPACE}/out
          cp /tmp/ReSukiSU-SUSFS-M14x-kernel.zip ${GITHUB_WORKSPACE}/out/
          ls -lh /tmp/ReSukiSU-SUSFS-M14x-kernel.zip

      - name: ⬆️ Upload Flashable Kernel Zip
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: ReSukiSU-SUSFS-M14x-kernel
          path: out/ReSukiSU-SUSFS-M14x-kernel.zip
          retention-days: 30

      - name: ⬆️ Upload Build Log (On Failure)
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: build-log-failure
          path: /tmp/kernel_build.log
          retention-days: 7
